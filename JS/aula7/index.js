const primeiroNumero = '5';
const segundoNumero = 10;
const resultado = primeiroNumero + segundoNumero;
console.log(resultado);
console.log(typeof(resultado));

/* const não pode ser alterado depois ao contrário do let.
const é necessário inicializar a variável com algum valor, caso contrário, o código não funcionará.
não é utilizar var, pois ela é uma variável global e pode ser alterada em qualquer lugar do código.
Um dos tipos é string, o outro é number, ao tentar somar uma string com um number, o resultado será uma string concatenada.
*/